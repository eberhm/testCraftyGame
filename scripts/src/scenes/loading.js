define('scenes/loading', function() {
    //here the scene code
    Crafty.scene('loading', function() {

    });

    var loader = {
        load : function() {
            Crafty.load('loading');
        }
    };

    return loader;
});